@extends('layouts.app')

@section('title', 'Payment Successful')

@section('content')
<div class="container mx-auto px-4 py-8">
    <div class="max-w-3xl mx-auto">
        <!-- Success Header -->
        <div class="text-center mb-8">
            <div class="w-20 h-20 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4">
                <svg class="w-10 h-10 text-green-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
            </div>
            <h1 class="text-3xl font-bold text-green-600 mb-2">🎉 Payment Successful!</h1>
            <p class="text-lg text-gray-600">Your booking has been confirmed successfully.</p>
        </div>

        <!-- Booking Details -->
        <div class="bg-white rounded-lg shadow-lg p-6 mb-6">
            <h2 class="text-2xl font-semibold mb-4 text-gray-800">📋 Booking Details</h2>
            
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div class="space-y-4">
                    <div>
                        <span class="text-sm font-medium text-gray-500 block">Booking ID</span>
                        <p class="text-lg font-semibold text-gray-900">#{{ $booking->id }}</p>
                    </div>
                    
                    <div>
                        <span class="text-sm font-medium text-gray-500 block">Package</span>
                        <p class="text-lg font-semibold text-blue-600">{{ $booking->package->name ?? 'Package' }}</p>
                    </div>
                    
                    <div>
                        <span class="text-sm font-medium text-gray-500 block">Travel Date</span>
                        <p class="text-lg font-semibold">
                            {{ \Carbon\Carbon::parse($booking->travel_date)->format('d M, Y') }}
                        </p>
                    </div>
                    
                    <div>
                        <span class="text-sm font-medium text-gray-500 block">Travelers</span>
                        <p class="text-lg font-semibold">{{ $booking->adults }} Adults, {{ $booking->children }} Children</p>
                    </div>
                </div>

                <div class="space-y-4">
                    <div>
                        <span class="text-sm font-medium text-gray-500 block">Customer Name</span>
                        <p class="text-lg font-semibold">{{ $booking->name }}</p>
                    </div>
                    
                    <div>
                        <span class="text-sm font-medium text-gray-500 block">Email</span>
                        <p class="text-lg">{{ $booking->email }}</p>
                    </div>
                    
                    <div>
                        <span class="text-sm font-medium text-gray-500 block">Phone</span>
                        <p class="text-lg">{{ $booking->phone }}</p>
                    </div>
                    
                    <div>
                        <span class="text-sm font-medium text-gray-500 block">Total Amount</span>
                        <p class="text-2xl font-bold text-green-600">₹{{ number_format($booking->total_amount, 2) }}</p>
                    </div>
                </div>
            </div>

            @if($booking->special_requests)
            <div class="mt-6 pt-6 border-t">
                <span class="text-sm font-medium text-gray-500 block mb-2">Special Requests</span>
                <p class="text-gray-700 bg-gray-50 p-3 rounded">{{ $booking->special_requests }}</p>
            </div>
            @endif
        </div>

        <!-- Payment Info -->
        <div class="bg-gray-50 rounded-lg p-6 mb-6">
            <h3 class="text-lg font-semibold mb-4 text-gray-800">💳 Payment Information</h3>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
                <div>
                    <span class="text-gray-500 block">Payment ID</span>
                    <p class="font-mono text-xs bg-white p-2 rounded border">{{ $booking->razorpay_payment_id }}</p>
                </div>
                <div>
                    <span class="text-gray-500 block">Order ID</span>
                    <p class="font-mono text-xs bg-white p-2 rounded border">{{ $booking->razorpay_order_id }}</p>
                </div>
                <div>
                    <span class="text-gray-500 block">Payment Date</span>
                    <p class="font-semibold">{{ $booking->paid_at ? $booking->paid_at->format('d M, Y h:i A') : now()->format('d M, Y h:i A') }}</p>
                </div>
                <div>
                    <span class="text-gray-500 block">Status</span>
                    <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-green-100 text-green-800">
                        ✓ Confirmed
                    </span>
                </div>
            </div>
        </div>

        <!-- Action Buttons -->
        <div class="text-center space-y-4 md:space-y-0 md:space-x-4">
            <a href="{{ route('my-bookings') }}" 
               class="w-full md:w-auto bg-blue-600 hover:bg-blue-700 text-white font-medium py-3 px-8 rounded-lg inline-flex items-center justify-center">
                <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v10a2 2 0 002 2h8a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path>
                </svg>
                View My Bookings
            </a>
            
            <a href="{{ route('home') }}" 
               class="w-full md:w-auto bg-gray-600 hover:bg-gray-700 text-white font-medium py-3 px-8 rounded-lg inline-flex items-center justify-center">
                <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v4a1 1 0 001 1m-6 0h6"></path>
                </svg>
                Back to Home
            </a>
        </div>

        <!-- Thank You Message -->
        <div class="text-center mt-8 p-6 bg-blue-50 rounded-lg">
            <h3 class="text-lg font-semibold text-blue-800 mb-2">Thank You! 🙏</h3>
            <p class="text-blue-700">
                Thank you for choosing MyBirBilling! We'll send you a confirmation email shortly with all the details.
                Our team will contact you soon regarding your {{ $booking->package->name ?? 'package' }} booking.
            </p>
        </div>
    </div>
</div>
@endsection
