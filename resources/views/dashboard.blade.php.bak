@extends('layouts.app')

@section('content')
<div class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100 py-12">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <!-- Header with Add Flight Button -->
        <div class="bg-white rounded-lg shadow-xl p-6 mb-8">
            <div class="flex justify-between items-center">
                <div>
                    <h1 class="text-3xl font-bold text-gray-800">Welcome back, {{ auth()->user()->name }}!</h1>
                    <p class="text-gray-600 mt-2">Your Paragliding Journey Dashboard</p>
                </div>
                <!-- ADD FLIGHT LOG BUTTON -->
                <a href="{{ route('dashboard.flight-logs.create') }}" 
                   class="bg-green-600 hover:bg-green-700 text-white font-bold py-3 px-6 rounded-lg flex items-center shadow-lg transform hover:scale-105 transition">
                    <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
                    </svg>
                    Add Flight Log
                </a>
            </div>
        </div>

        <!-- Quick Stats -->
        <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
            <div class="bg-white rounded-lg shadow p-6 hover:shadow-lg transition cursor-pointer" onclick="window.location.href='{{ route('dashboard.flight-logs') }}'">
                <div class="flex items-center">
                    <div class="p-3 bg-indigo-100 rounded-full">
                        <svg class="w-8 h-8 text-indigo-600" fill="currentColor" viewBox="0 0 20 20">
                            <path d="M10.894 2.553a1 1 0 00-1.788 0l-7 14a1 1 0 001.169 1.409l5-1.429A1 1 0 009 15.571V11a1 1 0 112 0v4.571a1 1 0 00.725.962l5 1.428a1 1 0 001.17-1.408l-7-14z"></path>
                        </svg>
                    </div>
                    <div class="ml-4">
                        <p class="text-gray-500 text-sm">Total Flights</p>
                        <p class="text-2xl font-semibold">{{ $stats->total_flights ?? 0 }}</p>
                    </div>
                </div>
            </div>

            <div class="bg-white rounded-lg shadow p-6 hover:shadow-lg transition">
                <div class="flex items-center">
                    <div class="p-3 bg-green-100 rounded-full">
                        <svg class="w-8 h-8 text-green-600" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"></path>
                        </svg>
                    </div>
                    <div class="ml-4">
                        <p class="text-gray-500 text-sm">Flight Hours</p>
                        <p class="text-2xl font-semibold">{{ $stats->total_flight_hours ?? 0 }}</p>
                    </div>
                </div>
            </div>

            <div class="bg-white rounded-lg shadow p-6 hover:shadow-lg transition" onclick="window.location.href='{{ route('dashboard.certificates') }}'">
                <div class="flex items-center">
                    <div class="p-3 bg-purple-100 rounded-full">
                        <svg class="w-8 h-8 text-purple-600" fill="currentColor" viewBox="0 0 20 20">
                            <path d="M10.394 2.08a1 1 0 00-.788 0l-7 3a1 1 0 000 1.84L5.25 8.051a.999.999 0 01.356-.257l4-1.714a1 1 0 11.788 1.838L7.667 9.088l1.94.831a1 1 0 00.787 0l7-3a1 1 0 000-1.838l-7-3z"></path>
                        </svg>
                    </div>
                    <div class="ml-4">
                        <p class="text-gray-500 text-sm">Certificates</p>
                <p class="text-2xl font-semibold">{{ $stats["certificates"] ?? 0 }}</p>
                    </div>
                </div>
            </div>

            <div class="bg-white rounded-lg shadow p-6 hover:shadow-lg transition" onclick="window.location.href='{{ route('dashboard.achievements') }}'">
                <div class="flex items-center">
                    <div class="p-3 bg-yellow-100 rounded-full">
                        <svg class="w-8 h-8 text-yellow-600" fill="currentColor" viewBox="0 0 20 20">
                            <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"></path>
                        </svg>
                    </div>
                    <div class="ml-4">
                        <p class="text-gray-500 text-sm">Achievements</p>
                        <p class="text-2xl font-semibold">{{ $recentAchievements->count() ?? 0 }}</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Quick Links with better styling -->
        <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
            <a href="{{ route('dashboard.certificates') }}" class="bg-white rounded-lg shadow p-6 text-center hover:shadow-xl transition transform hover:scale-105">
                <span class="text-4xl">📜</span>
                <p class="mt-2 text-gray-700 font-semibold">Certificates</p>
            </a>
            <a href="{{ route('dashboard.gallery') }}" class="bg-white rounded-lg shadow p-6 text-center hover:shadow-xl transition transform hover:scale-105">
                <span class="text-4xl">📸</span>
                <p class="mt-2 text-gray-700 font-semibold">Gallery</p>
            </a>
            <a href="{{ route('dashboard.achievements') }}" class="bg-white rounded-lg shadow p-6 text-center hover:shadow-xl transition transform hover:scale-105">
                <span class="text-4xl">🏆</span>
                <p class="mt-2 text-gray-700 font-semibold">Achievements</p>
            </a>
            <a href="{{ route('dashboard.statistics') }}" class="bg-white rounded-lg shadow p-6 text-center hover:shadow-xl transition transform hover:scale-105">
                <span class="text-4xl">📊</span>
                <p class="mt-2 text-gray-700 font-semibold">Statistics</p>
            </a>
        </div>

        <!-- Recent Flight Logs Section -->
        @if($recentFlightLogs && $recentFlightLogs->count() > 0)
        <div class="bg-white rounded-lg shadow-xl p-6 mt-8">
            <div class="flex justify-between items-center mb-4">
                <h2 class="text-xl font-bold text-gray-800">Recent Flights</h2>
                <a href="{{ route('dashboard.flight-logs') }}" class="text-indigo-600 hover:text-indigo-900">View All →</a>
            </div>
            <div class="overflow-x-auto">
                <table class="min-w-full divide-y divide-gray-200">
                    <thead>
                        <tr>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Date</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Site</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Duration</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Max Altitude</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-200">
                        @foreach($recentFlightLogs as $log)
                        <tr>
                            <td class="px-6 py-4 whitespace-nowrap">{{ $log->date->format('M d, Y') }}</td>
                            <td class="px-6 py-4 whitespace-nowrap">{{ $log->site_name }}</td>
                            <td class="px-6 py-4 whitespace-nowrap">{{ $log->flight_duration }} min</td>
                            <td class="px-6 py-4 whitespace-nowrap">{{ $log->max_altitude }} m</td>
                        </tr>
                        @endforeach
                    </tbody>
                </table>
            </div>
        </div>
        @endif
    </div>
</div>
@endsection
