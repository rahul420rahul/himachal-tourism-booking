<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
    <meta name="csrf-token" content="{{ csrf_token() }}">
    <meta name="theme-color" content="#3B82F6">
    <title>{{ config('app.name', 'MyBirBilling') }} - @yield('title', 'Paragliding Adventures')</title>
    
    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}">
    
    <!-- Fonts -->
    <link href="https://fonts.bunny.net/css?family=inter:300,400,500,600,700,800&display=swap" rel="stylesheet">
    
    <!-- Styles -->
    @vite(['resources/css/app.css', 'resources/js/app.js'])
    
    <!-- AOS Animation -->
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    
    <style>
        [x-cloak] { display: none !important; }
        
        html {
            scroll-behavior: smooth;
            -webkit-tap-highlight-color: transparent;
        }
        
        body {
            overscroll-behavior-y: none;
        }
        
        @media (max-width: 1024px) {
            body {
                padding-top: 64px;
                padding-bottom: 70px;
            }
            
            main {
                min-height: calc(100vh - 134px);
            }
        }
        
        @media (min-width: 1025px) {
            body {
                padding-top: 0;
                padding-bottom: 0;
            }
        }
        
        .bottom-nav-shadow {
            box-shadow: 0 -4px 6px -1px rgba(0, 0, 0, 0.1);
        }
        
        @supports (backdrop-filter: blur(20px)) {
            .blur-bg {
                background-color: rgba(255, 255, 255, 0.8);
                backdrop-filter: blur(20px);
                -webkit-backdrop-filter: blur(20px);
            }
        }
        
        .nav-active {
            color: #3B82F6;
            transform: translateY(-2px);
        }
        
        .no-scroll {
            overflow: hidden;
            position: fixed;
            width: 100%;
        }
        
        /* Mobile menu slide animation */
        .menu-slide-enter {
            transform: translateX(100%);
        }
        .menu-slide-enter-active {
            transform: translateX(0);
            transition: transform 0.3s ease-out;
        }
        .menu-slide-leave {
            transform: translateX(0);
        }
        .menu-slide-leave-active {
            transform: translateX(100%);
            transition: transform 0.3s ease-in;
        }
    </style>
    @stack('styles')
</head>

<body class="font-sans antialiased bg-gray-50" x-data="{ mobileMenuOpen: false, scrolled: false, showBottomNav: true, lastScrollY: 0 }" 
      @scroll.window="
          scrolled = window.pageYOffset > 50;
          if (window.innerWidth < 1024) {
              showBottomNav = window.pageYOffset < lastScrollY || window.pageYOffset < 100;
              lastScrollY = window.pageYOffset;
          }
      "
      :class="{ 'no-scroll': mobileMenuOpen }">
    
    <!-- Navigation Header (Responsive) -->
    @include('partials.header')
    <nav class="hidden lg:block">
        @include('partials.header')
    </nav>
    
    <!-- Mobile Slide Menu -->
    <div x-show="mobileMenuOpen" 
         x-cloak
         @click.outside="mobileMenuOpen = false"
         x-transition:enter="transition ease-out duration-300"
         x-transition:enter-start="transform translate-x-full"
         x-transition:enter-end="transform translate-x-0"
         x-transition:leave="transition ease-in duration-200"
         x-transition:leave-start="transform translate-x-0"
         x-transition:leave-end="transform translate-x-full"
         class="mobile-menu-panel lg:hidden fixed inset-y-0 right-0 z-50 w-80 bg-white shadow-2xl overflow-y-auto translate-x-full transition-transform duration-300">
        
        @include('partials.mobile-menu-content')
    </div>
    
    <!-- Mobile Menu Overlay -->
    <div x-show="mobileMenuOpen" 
         x-cloak
         @click="mobileMenuOpen = false"
         x-transition:enter="transition-opacity ease-linear duration-300"
         x-transition:enter-start="opacity-0"
         x-transition:enter-end="opacity-100"
         x-transition:leave="transition-opacity ease-linear duration-300"
         x-transition:leave-start="opacity-100"
         x-transition:leave-end="opacity-0"
         class="menu-overlay lg:hidden fixed inset-0 z-40 bg-black bg-opacity-50 hidden"></div>
    
    <!-- Hero Section -->
    @hasSection('hero')
        <section class="hero-section">
            @yield('hero')
        </section>
    @endif
    
    <!-- Main Content -->
    <main id="main-content" class="flex-grow">
        @yield('content')
    </main>
    
    <!-- Footer -->
    <footer class="hidden lg:block">
        @include('partials.footer')
    </footer>
    
    <!-- Mobile Bottom Navigation -->
    <nav class="lg:hidden fixed bottom-0 left-0 right-0 z-40 blur-bg bottom-nav-shadow transition-transform duration-300"
         :class="{ 'translate-y-full': !showBottomNav }">
        <div class="grid grid-cols-5 gap-1 px-2 py-2">
            <!-- Home -->
            <a href="{{ route('home') }}" 
               class="flex flex-col items-center py-2 px-1 rounded-lg transition-all {{ request()->routeIs('home') ? 'nav-active' : 'text-gray-600' }}">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
                </svg>
                <span class="text-xs mt-1">Home</span>
            </a>
            
            <!-- Packages -->
            <a href="{{ route('packages.index') }}" 
               class="flex flex-col items-center py-2 px-1 rounded-lg transition-all {{ request()->routeIs('packages.*') ? 'nav-active' : 'text-gray-600' }}">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"/>
                </svg>
                <span class="text-xs mt-1">Packages</span>
            </a>
            
            <!-- Book Now (Center) -->
            <a href="{{ route('packages.index') }}" 
               class="flex flex-col items-center py-1 px-1 -mt-2">
                <div class="w-14 h-14 bg-gradient-to-r from-blue-600 to-purple-600 rounded-full flex items-center justify-center shadow-lg transform hover:scale-110 transition-all">
                    <svg class="w-7 h-7 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"/>
                    </svg>
                </div>
                <span class="text-xs mt-1 font-semibold text-gray-900">Book</span>
            </a>
            
            <!-- Gallery -->
            <a href="{{ route('gallery') }}" 
               class="flex flex-col items-center py-2 px-1 rounded-lg transition-all {{ request()->routeIs('gallery') ? 'nav-active' : 'text-gray-600' }}">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                </svg>
                <span class="text-xs mt-1">Gallery</span>
            </a>
            
            <!-- Profile/Login -->
            @auth
            <a href="{{ route('profile.edit') }}" 
               class="flex flex-col items-center py-2 px-1 rounded-lg transition-all {{ request()->routeIs('profile.*') ? 'nav-active' : 'text-gray-600' }}">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                </svg>
                <span class="text-xs mt-1">Profile</span>
            </a>
            @else
            <a href="{{ route('login') }}" 
               class="flex flex-col items-center py-2 px-1 rounded-lg transition-all text-gray-600">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1"/>
                </svg>
                <span class="text-xs mt-1">Login</span>
            </a>
            @endauth
        </div>
    </nav>
    
    <!-- Floating WhatsApp Button -->
    <a href="https://wa.me/919736696260" 
       target="_blank"
       class="lg:hidden fixed bottom-20 right-4 z-30 w-14 h-14 bg-green-500 rounded-full flex items-center justify-center shadow-lg hover:scale-110 transition-all">
        <svg class="w-8 h-8 text-white" fill="currentColor" viewBox="0 0 24 24">
            <path d="M.057 24l1.687-6.163c-1.041-1.804-1.588-3.849-1.587-5.946.003-6.556 5.338-11.891 11.893-11.891 3.181.001 6.167 1.24 8.413 3.488 2.245 2.248 3.481 5.236 3.48 8.414-.003 6.557-5.338 11.892-11.893 11.892-1.99-.001-3.951-.5-5.688-1.448l-6.305 1.654zm6.597-3.807c1.676.995 3.276 1.591 5.392 1.592 5.448 0 9.886-4.434 9.889-9.885.002-5.462-4.415-9.89-9.881-9.892-5.452 0-9.887 4.434-9.889 9.884-.001 2.225.651 3.891 1.746 5.634l-.999 3.648 3.742-.981z"/>
        </svg>
    </a>
    
    <!-- Alpine.js (only once at the end) -->
    
    <!-- AOS Init -->
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script>
        AOS.init({
            duration: 800,
            once: true,
            offset: 100
        });
    </script>
    
    @stack('scripts')
</body>
</html><script>
document.addEventListener('DOMContentLoaded', function() {
    // Mobile menu toggle
    const menuBtn = document.querySelector('.menu-toggle-btn');
    const mobileMenu = document.querySelector('.mobile-menu-panel');
    const menuOverlay = document.querySelector('.menu-overlay');
    
    if(menuBtn && mobileMenu) {
        menuBtn.addEventListener('click', function() {
            mobileMenu.classList.toggle('translate-x-full');
            menuOverlay.classList.toggle('hidden');
            document.body.classList.toggle('overflow-hidden');
        });
        
        if(menuOverlay) {
            menuOverlay.addEventListener('click', function() {
                mobileMenu.classList.add('translate-x-full');
                menuOverlay.classList.add('hidden');
                document.body.classList.remove('overflow-hidden');
            });
        }
    }
});
</script>
</body>
</html>
